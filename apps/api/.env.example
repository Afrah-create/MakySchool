PORT=4000
CORS_ORIGIN=http://localhost:3000

# ── Supabase PostgreSQL ───────────────────────────────────────────────────────
# Project Settings → Database → Connection string → URI
# Use "Session pooler" for the API runtime (recommended).
DATABASE_URL=postgresql://postgres.xcvenxemrkafdygjvdwi:Afratkip14#@aws-0-eu-west-3.pooler.supabase.com:5432/postgres

# Optional: force SSL when not using a supabase.co host
# DATABASE_SSL=true

# ── JWT secrets (required in production) ──────────────────────────────────────
SUPERADMIN_JWT_SECRET=change-me-superadmin
TENANT_JWT_SECRET=change-me-tenant

# Run migrations on API start in production (default: auto in dev, manual in prod)
# RUN_MIGRATIONS=true

# ── Super admin seed (run after migrate: npm run seed) ────────────────────────
SUPERADMIN_EMAIL=admin@makyschool.com
SUPERADMIN_PASSWORD=ChangeMe123!
SUPERADMIN_NAME=Platform Admin
# SUPERADMIN_FORCE_RESET=true
